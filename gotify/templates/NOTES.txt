Gotify notification server has been deployed!

{{- if .Values.ingress.enabled }}
{{- range .Values.ingress.hosts }}

Access Gotify at: http://{{ .host }}
{{- end }}

{{- else }}

To access Gotify, port-forward the service:
  kubectl port-forward svc/{{ include "gotify.fullname" . }} 8080:{{ .Values.service.port }}

Then visit: http://localhost:8080
{{- end }}

Default credentials:
  Username: {{ range .Values.env }}{{ if eq .name "GOTIFY_DEFAULTUSER_NAME" }}{{ .value }}{{ end }}{{ end }}
  Password: {{ range .Values.env }}{{ if eq .name "GOTIFY_DEFAULTUSER_PASS" }}{{ .value }}{{ end }}{{ end }}

IMPORTANT: Change the default password after first login!

{{- if .Values.persistence.enabled }}

Data persistence is enabled. Your Gotify data will be stored in:
  PVC: {{ if .Values.persistence.existingClaim }}{{ .Values.persistence.existingClaim }}{{ else }}{{ include "gotify.fullname" . }}-data{{ end }}
{{- else }}

WARNING: Persistence is disabled. Your data will be lost if the pod restarts!
{{- end }}

For more information, visit: https://gotify.net/docs/
