File Browser has been deployed!

{{- if .Values.ingress.enabled }}

Access File Browser at: http://{{ .Values.ingress.host }}

{{- else }}

To access File Browser, port-forward the service:
  kubectl port-forward svc/{{ include "filebrowser.fullname" . }} 8080:{{ .Values.service.port }}

Then visit: http://localhost:8080
{{- end }}

Default credentials:
  Username: admin
  Password: admin

IMPORTANT: Change the default password after first login!

Configuration stored in:
  PVC: {{ include "filebrowser.fullname" . }}-config
  Path: /config

{{- if .Values.persistence.media.enabled }}

Browsing media files from:
  PVC: {{ .Values.persistence.media.existingClaim }}
  Path: /media

✓ You can now manage files downloaded by MeTube and served by Jellyfin
{{- end }}

File Browser features:
- Upload, download, and manage files
- Create, edit, and delete files and folders
- Share files with direct links
- File search and preview
- User management and permissions
