Glance dashboard has been deployed!

{{- if .Values.ingress.enabled }}

Access Glance at: http://{{ .Values.ingress.host }}

{{- else }}

To access Glance, port-forward the service:
  kubectl port-forward svc/{{ include "glance.fullname" . }} 8080:{{ .Values.service.port }}

Then visit: http://localhost:8080
{{- end }}

Mount your glance.yml config into the persistent volume at /app/config/glance.yml

