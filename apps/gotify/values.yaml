replicaCount: 1

image:
  repository: gotify/server
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  className: "traefik"
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
  host: gotify
  paths:
    - path: /
      pathType: Prefix

persistence:
  enabled: true
  storageClass: "local-path"
  accessMode: ReadWriteOnce
  size: 1Gi

env:
  - name: GOTIFY_DEFAULTUSER_NAME
    valueFrom:
      secretKeyRef:
        name: gotify-user-secret
        key: username
  - name: GOTIFY_DEFAULTUSER_PASS
    valueFrom:
      secretKeyRef:
        name: gotify-user-secret
        key: userpass
